// ignore_for_file: avoid_print

import 'dart:io';

class PostMethods {
  Future<String> addPost(
    String caption,
    File image,
    String uid,
    String userName,
    String avatar,
  ) async {
    try {} catch (err) {
      print(err);
    }

    return "";
  }
}
